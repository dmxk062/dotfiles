# vim: ft=hyprland.conf
bind = ALT, TAB, hycov:toggleoverview
plugin {
hycov {
    enable_hotarea = 0
    only_active_monitor = 1 
    auto_exit = 1
    only_active_workspace = 1
    enable_gesture = 1
}
}
