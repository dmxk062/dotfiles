# vim: ft=hyprland.conf
plugin = /home/dmx/.config/hypr/plugins/hyprbars.so
plugin {
    hyprbars {
        bar_height = 40
        bar_color = rgba(2e3440aa)
        bar_text_font = Torus Pro
        col.text: $foreground
        hyprbars-button = rgb(bf616a), 20, 󰖭, hyprctl dispatch killactive
    }
}
