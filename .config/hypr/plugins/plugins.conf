# vim: ft=hyprlang

exec-once = hyprpm reload
$button_size = 20

# source = $configdir/plugins/gestures.conf
# source = $configdir/plugins/overview.conf
# source = $configdir/plugins/expo.conf
# source = $configdir/plugins/hyprspace.conf
