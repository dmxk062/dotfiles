(defwindow desktop_area 
        :geometry (geometry :x 0
                            :y 0
                            :width "100%"
                            :height "100%"
                    )
        :stacking "bg"
        :namespace "eww_rightclick"
    (desktop)
)

(defwidget desktop []
(overlay
;; (box
;; (desktop-clock)
;; )
(rightclick_menu)
)
)

;; (include "desktop/widgets/clock.yuck")
(include "desktop/rightclick.yuck")
