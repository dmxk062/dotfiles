(include "bar/win.yuck")
(include "bar/mpris.yuck")
(include "bar/audio.yuck")
;; (include "bar/dragndrop.yuck")
(include "bar/submap.yuck")




(defwidget center []
    (box :space-evenly false  :halign "center"
        (box :space-evenly false :class "centerbox"
            ;; (dragndrop_area)
            (submap)
            (window)
            (mpris_button)
            ;; (audio_button)
        )
    )
)
