(defvar overview false)

(defwidget overview_button []
    (button :class "sbutton${overview ? "-active" : ""}" :timeout "128s"
    :onclick "sleep 0.1; ~/.config/hypr/plugins/overview.sh"
    "")
)
