(defvar sources "[]")
(defvar sinks "[]")
(defvar active_sinks '{}')
(defvar active_sources '{}')
(defvar audio_state '{}')

(include "../settings/icons/audio.yuck")


(defwidget audio_button []
    (box :space-evenly false 
        (button :class "sbutton" :timeout "16s" :onclick "pamixer -t&&../settings/bin/audio_state.sh" "${audio_state.sink.mute ? sink_icons[active_sinks.icon].muted : sink_icons[active_sinks.icon].unmuted}    ${audio_state.sink.vol}%")
    )
)
