; extends
(header_email) @property
(header_separator) @punctuation.delimiter
(atom) @variable
(email) @string

(header_field_subject) @property
(header_subject 
  (header_unstructured) @markup.heading)
